#include<stdio.h>
#include<conio.h>
void main()
{
int i,n,sum;
printf("enter the value");
scanf("%d%d%d",&i,&n,&sum);
for(i=1;i<=n;i++)
{
sum=sum+i;
}
printf("the sum of the number is%d,sum);
return 0;
}
